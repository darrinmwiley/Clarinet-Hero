class Prediction:
    def __init__(self, snippetStartTimeMs, snippetEndTimeMs, noteActivations):
        self.snippetStartTimeMs = snippetStartTimeMs
        self.snippetEndTimeMs = snippetEndTimeMs
        self.noteActivations = noteActivations
